# 版本迭代 (Version History)

> 记录本项目从上游开源仓库到个人改造的版本演进。
> **0.0** 为最初的上游仓库基线;**中间小版本**对应逐项加入的功能点(详见各版本所列 commit);
> **大版本 1.0** 预计在「全部功能完善 + 图形化界面改造完成」后发布。
>
> 说明:本项目 fork 自开源的 **Embabel Tripper**。`0.0` 为上游基线,`0.1` 起为个人扩展。
> 个人改造的功能阶段与 `README-AI-APPLICATION-PLAN.md` 的 Phase 规划一一对应。

---

## 0.0 — 上游基线 (Upstream Baseline)
*2025-06-10 ~ 2026-01-06 · 继承自 Embabel Tripper*

开源仓库原有能力,非个人工作:

- Kotlin + Spring Boot 的 Embabel Agent 工作流(多 `@Action` 步骤)。
- 旅行领域模型:travel brief、travelers、points of interest、days、stays、travel plan。
- 工具编排:Web 搜索、地图、天气、图片、Airbnb(MCP)。
- htmx Web UI、SSE 流式、Neo4j(OGM/向量)、Docker Compose、Anthropic/OpenAI 模型。
- 关键 commit:`699592b Initial commit` → `066c789 Add htmx interface` → `67cff13 Journey planning async` → `6c67e88 Stays` → `d736cf9 Promote planning to top level page` → `d477814 Add security` → `67989ac Upgrade to Spring Boot 3.5.9 and Embabel 0.3.2`。

## 0.1 — 作品集基线与 CI (Phase 0)
*2026-03-14*

- GitHub Actions Java CI(Maven 构建 + 单元测试)、构建通知、`repository_dispatch` 触发。
- 项目卫生文档:`PROJECT-NOTE.md`、`README-AI-APPLICATION-PLAN.md`、`infra.md`、`LOCAL-DEVELOPMENT.md`、Maven Wrapper、demo 脚本。
- commit:`5805f71 Add GitHub Actions workflow` · `7b0a96e Add Build Notifications` · `002a08c Add repository_dispatch trigger`。

## 0.2 — RAG 旅行知识库 (Phase 1)
*2026-06-04*

- Java 实现的 RAG MVP(`com.embabel.tripper.rag`):文档导入(文本/文件/URL)、分块、检索、`[KB:id]` 引用注入 Agent 提示词、检索调试页。
- 当前检索为 TF 词向量 + 余弦(待升级为真 embedding + 向量库,见 1.0 路线)。
- commit:`9535f2b Add Java travel knowledge base phase`。

## 0.3 — 行程校验与修复回路 (Phase 2)
*2026-06-04*

- Java 校验器(`com.embabel.tripper.verification`):日期覆盖、缺失地点、路线 haversine 估算、预算、链接、住宿覆盖等结构化校验;一次性 Agent 修复回路;UI 校验摘要;单元测试。
- commit:`54c5272 Add itinerary verifier` · `42e19c1 Fix verifier compilation`。

## 0.4 — 评估框架 (Phase 3)
*2026-06-05*

- Java 评估 MVP(`com.embabel.tripper.eval`):30 个旅行用例数据集、确定性回归指标、JSON/Markdown 报告、CI 友好单测。
- commit:`08c0aac Add evaluation harness`。

## 0.5 — 运行可观测性 / AgentOps (Phase 4)
*2026-06-06*

- Java 可观测 MVP(`com.embabel.tripper.observability`):按 run 的 action 时间线、模型/工具/token/成本捕获、成本告警、`/runs` 轨迹页。
- commit:`0a962f4 Add run observability`。

## 0.6 — 安全护栏 (Phase 5)
*2026-06-07*

- Java 安全 MVP(`com.embabel.tripper.safety`):prompt 注入检测、不可信 RAG 内容包装、工具策略提示、不安全链接过滤、轨迹脱敏。
- commit:`0944de3 Add safety guardrails`。

## 0.7 — 多轮计划编辑 (Phase 6)
*2026-06-07*

- Java 编辑 MVP(`com.embabel.tripper.editing`):计划版本化、范围化编辑备注、日级 diff、约束保留、编辑后重跑校验。
- commit:`cb37517 Add plan editing`。

## 0.8 — 可运行性、成本护栏与国际化
*2026-06-09*

- **可运行性**:新增 `application-gateway.yml`(SSE 连本地 MCP 网关 9011),解决默认 `DOCKER_DESKTOP` 路径连 0 个 client、空烧 token 的问题;补齐 `.env.local` 密钥并加入 `.gitignore`;实测端到端跑通完整 plan。
- **i18n 中英文切换**:`I18nConfig.kt` + `messages*.properties` + 右上角语言开关;入口/结果/页脚模板外部化;修复 `fallback-to-system-locale` 在中文系统下的坑。
- **行程正文跟随 UI 语言**:`JourneyTravelBrief.language` + 控制器按 locale 注入 + Agent 提示词指令(地名/URL 保持拉丁格式)。
- **工具修复**:`Fix Brave search URL`(图搜 URL 拼接 bug)、`Add web and maps tool groups`(接上网关的网页搜索/地图)。
- **成本护栏**:`Trim Brave image search output`、`Cap tool calls and points of interest`、`Scale points of interest by trip days`、`Reduce LLM retry attempts`、`Drop maps from proposal and lower POI count` —— 把一次失控的 10x token / 42 分钟卡死压成 13 分钟出图、$0.52。
- **遗留**(见 `项目问题记录.md` #13–16):propose 跨境 EOF 慢(根治靠 #10)、编辑页 500、weather 组空、进度反馈 UI。

## 0.9 — RAG 升级:本地 embedding + 向量检索
*2026-06-10*

- `TravelKnowledgeService` 从 **TF 词频 + 余弦(关键词)** 换成 **真 embedding + 向量检索**,检索对外接口不变(agent 未动)。
- **本地 ONNX** embedding(Spring AI `TransformersEmbeddingModel` / all-MiniLM-L6-v2,离线、免费、不跨境)+ 内存 `SimpleVectorStore`(`RagConfig`,pom 加 `spring-ai-transformers`)。
- 可调旋钮 `RagProperties`(chunk 大小/重叠、topK、相似度阈值),留作可量化优化。
- 测试:离线确定性假 embedding 跑管线/安全 + 一条真模型**语义测试**(同义词检索证明升级);25 测试全绿。
- commit:`Add local embedding model`、`Replace keyword retrieval with vector search`。
- 坐实简历 "embeddings, vector retrieval";后续优化:eval 驱动调参、混合检索、持久化向量库、国内 embedding API。

## 0.10 — 按语言路由到境内大模型(Kimi/Moonshot)
*2026-06-10*

- **按 UI 语言切模型**:中文 → 月之暗面 Kimi/Moonshot(境内可直连,根治跨境 EOF);英文 → Claude。`MoonshotModelsConfig` 经 `OpenAiCompatibleModelFactory` 注册模型;`TripperAgent` 按 `brief.language` 用 `withLlm` 覆盖(persona 保留)。
- **攻克 Moonshot 结构化输出不稳**(真实工程故事):
  - planner 输出**拆成两次调用** —— 短结构 JSON(`ProposedTravelPlanMeta`)+ 纯文本 HTML(`generateText`),绕开"HTML 塞 JSON"导致的非法 JSON。
  - 结构字段**全加默认值**,容忍模型间歇漏字段(`MissingKotlinParameterException`)。
  - 研究输出同样加固,并用输入 POI **强制回填** `pointOfInterest`。
  - **代码补全日期**,根除 DATE_GAP → 不触发 repair。
  - 去掉 `generateText` 偶发的 ```html markdown 围栏。
- **实测(London→Chester 中文)**:正文中文、校验 PASSED、4 张图、**$0.45 / 1分54秒**(对比 Claude 跨境 $0.52 / 13.5 分钟)。
- **repair 回路同构拆分**:`verifyAndRepairTravelPlan` 也改为两阶段(结构 JSON + 纯文本 HTML)+ 去工具 + 代码兜底日期,与 propose 一致——中文触发 repair 不再因 HTML-in-JSON 崩,境内路径两条写作路径全覆盖。
- commit:`Add Moonshot models` … `Generate plan as structure plus plain HTML`、`Default plan metadata fields`、`Make research output tolerant of dropped fields`、`Fill missing itinerary dates in code`、`Strip markdown code fence from plan HTML`、`Split plan repair into structure and HTML`。
- 坐实简历"模型可插拔 + 境内合规部署 + 根治跨境延迟"。

---

## 1.0 — 计划中 (Planned)
*目标:功能全部完善 + 图形化界面改造完成*

发布门槛(详见 `项目问题记录.md` 待办项):

- [ ] **图形化可交互结果网站**:景点以像素化 SVG 等形式作为可交互点位,点击展开概要(第 11 点)。
- [x] **RAG 升级**:关键词检索 → 真 embedding + 向量检索(第 1 点)—— 0.9 完成。
- [x] **Brave URL bug 修复**:图片搜索真正生效(第 8 点)—— 0.8 完成。
- [ ] **工具补全**:`web`/`maps` 工具组与 Cost 字段已在 0.8 完成;仍欠 weather 来源、编辑页 500(第 6/15 点)。
- [x] **延迟根治**:中文路径走境内 Kimi,跨境 EOF 消失(第 16 点)—— 0.10 完成。
- [x] **中国境内部署(LLM)**:中文按语言路由到 Kimi/Moonshot(第 10 点)—— 0.10 完成;embedding/工具/镜像的境内化为后续。
- [ ] **i18n 收尾**:剩余页面静态文案翻译完整。
- [ ] **评估升级**:Agent-backed 候选 + LLM-judge;补 Agent 流程集成测试。
